apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enquiry-app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enquiry-app-service
            port:
              number: 80
